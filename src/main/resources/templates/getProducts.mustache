<!DOCTYPE html>
<html lang="ru" xmlns:th="http://www.thymeleaf.org">
<head>

    <meta charset="UTF-8">
    <title>addProduct</title>
    <link rel="stylesheet" th:href="@{/styles/style.css}">
    <!--    <link rel="icon" th:href="@{/styles/logo.ico}" type="image/x-icon">-->

    <!-------TEST------------>
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/2.2.4/jquery.min.js"></script>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css">
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/js/bootstrap.min.js"></script>
    <link rel="stylesheet" th:href="@{/styles/mytest.css}">
    <!-------TEST------------>
</head>
<div class="result-cont container">
<script src="https://cdnjs.cloudflare.com/ajax/libs/mustache.js/4.2.0/mustache.min.js"></script>
{{#products}}
    <div class="found-product">
        <span> {{name}}</span>
        <i>{{calories}}</i>
        <i>{{bel}}</i>
        <i>{{fats}}</i>
        <i>{{ch}}</i>
        <span>{{servingSize}} {{servingUnit.name}}</span>
        <input type="number" placeholder="Количество" id="quantity">
        <span>{{servingUnit.name}}</span>
        <button class="addProductButton" data-product-id="{{id}}" data-product-unit="{{servingUnit}}" >Добавить</button>
        <span class="productAddedMessage" style="display:none;">Продукт добавлен</span>
    </div>

{{/products}}


{{^products}}
        <form method="post" action="add">
            <label>
                <input type="text" name="name" placeholder="Введите название продукта" />
            </label>
            <label>
            <input type="text" name="calories" placeholder="Введите калорийность" />
            </label>
            <label>
            <input type="text" name="bel" placeholder="Введите количество белков" /></label>
            <label>
            <input type="text" name="fats" placeholder="Введите количество жиров" /></label>
            <label>
            <input type="text" name="ch" placeholder="Введите количество углеводов" /></label>
            <label>
            <input type="text" name="servingSize" placeholder="Введите размер порции" /></label>
            <label>
                <select name="servingUnit">
                    <option value="">-- Выберите единицу измерения --</option>
                    {{#servingUnits}}
                        <option value="{{id}}">{{name}}</option>
                    {{/servingUnits}}
                </select></label>
            <button type="submit">Добавить</button>
        </form>
{{/products}}
<button class="hideProductButton">Скрыть</button>
</div>
